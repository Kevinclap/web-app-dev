// package-lock  evita descargar duplicidad de modulos
// A veces entre depencias y dependencias, se pudiesen repetir.
// package lock esvita que esto suceda

// npm install -g es una instalacion global ej. mocha
// yarn es para paquetes locales

// Una clase si guarda estado de su funcion vs fuciones regulares

 const AlbumGenerator = require('./albumGenerator')

 const myAlbum = new AlbumGenerator

	











	// artist(name)(string)
	// song(name)(string)
	// playtime(number)
	// createdAt (date)



	// function(setArtist)
	// function(setSong)
	// function(setPlaytime)
	// toJSON(function)
}